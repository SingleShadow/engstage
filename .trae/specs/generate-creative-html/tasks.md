# Tasks

- [x] Task 1: 生成 SpeakFlow 创意产物 HTML 文件
  - 创建 `creative-product.html`，一个自包含的单页展示文件
  - 包含以下 8 个内容板块：
    1. Hero 区域：产品名称 "SpeakFlow"、Slogan、渐变背景动画
    2. 痛点与解决方案：4 个用户痛点卡片 + 对应解决方案
    3. 核心功能展示：打字跟练模式 + 语音跟练模式的交互示意
    4. 场景库展示：5 大类别、19 个场景的网格卡片布局
    5. 分支对话亮点：用咖啡店场景展示分支对话流程图
    6. 游戏化系统：金币、成就、等级的可视化展示
    7. 产品界面 Mockup：用 CSS 绘制的手机/浏览器框架内嵌界面示意
    8. 页脚：目标用户总结 + 产品价值
  - 设计要求：
    - 深色主题（#0F172A 背景）
    - 蓝紫渐变主色调（#4F46E5 → #7C3AED）
    - 滚动渐入动画（Intersection Observer）
    - 响应式布局（flex/grid + media queries）
    - 纯内联 CSS + JS，零外部依赖
  - 技术实现：
    - 使用 CSS Grid / Flexbox 布局
    - 使用 Intersection Observer API 实现滚动动画
    - 使用 CSS 变量管理主题色
    - 使用 emoji 作为图标（无需外部图标库）

# Task Dependencies
- 无依赖，Task 1 可直接开始
